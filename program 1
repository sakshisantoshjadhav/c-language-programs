Q.Write a C' program to accept an integer and check if it is divisible by 3 and 5.

#include<stdio.h>
int main()
{
  int n;
  printf("enter number");
  scanf("%d",&n);
  if(n%3==0 && n%5==0)
  printf("number is divisible by 3 and 5");
  else
  printf("number is not divisible by 3 and 5");
}

